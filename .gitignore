node_modules/
dist/
.env
.env.local
.DS_Store
.gemini/
*.log
dev_files/
SCRATCH-NOTEPAD.md
dev_files/NOTEPAD
terminals/
mcps/

# Personal data exports — never commit backups
combatos-backup-*.json

# Review screenshots — working material, not repo history. Anything that
# becomes BINDING design evidence for a worker prompt should be committed
# explicitly with `git add -f` (as trw-app-more-tab.jpeg was for W29), so the
# prompt can point at a stable path.
archive/Snippets-for-review/
