## Current state (one line)
Combat OS `main` at `9547366` contains the accepted Track A application work and reconciled Track B
documentation; the standalone onboarding site is locally checkpointed at `a223b9e`.

## Pending

- [ ] **Choose one next Combat OS roadmap item:** start a task-specific branch from clean `main`;
      do not create a permanent model-specific development branch.
- [ ] **Final folder cleanup:** remove the empty `Fight-Camp-kimi-trial` directory after the old
      Codex task releases its Windows handle.
- [ ] **Onboarding site:** finish the remaining questionnaire feedback, reconcile Pages versus
      Workers hosting, then separately design auth and persistence.
- [ ] **Migration remains gated:** the draft onboarding migration is preserved but unapplied;
      resolve privacy, retention, state-transition, versioning, and webhook-boundary concerns first.
- [ ] **Security follow-up:** rotate the temporary Supabase developer password after app work.
