## Current state (one line)
A9 and A10 are merged and live: assigned-only Library, active-programme Plan, and the existing Today
HUD now form Train’s three-tab IA without changing the five-button main navigation. Final A10
verification: 369 tests, production PWA build and phone review passed. Today remains deliberately
legacy/playbook-driven until A6.5 and A7.

## Pending

- [ ] **A6.5 — durable active-workout draft:** begin in a fresh task with a read-only diagnostic.
      Specify the Dexie shape, cartridge/day/version identity, debounce and visibility flushes,
      restore/discard UX, successful-log clearing, sign-out handling, cartridge-switch conflicts,
      offline/PWA update behaviour and schema-upgrade tests. Do not change code before approval.
- [ ] **Rotate the developer Supabase password-login user’s password:** replace the temporary
      placeholder with something long and random.
- [ ] **Lock the permanent cartridge-session payload:** resolve prescribed/performed/substituted
      representation and per-session versus per-set shape after the relevant W26 decision work.
- [ ] **A7 — interactive cartridge renderer:** start only after A6.5 and the payload lock. Today
      must render and log the active cartridge while preserving flexible day-template selection.
- [ ] **Future independent diagnostics:** Exercise Reference layer with optional external demo links,
      followed later by an Academy/Exercise Guides grouped surface inside the layered navigation;
      Log-hub redesign remains separate.
